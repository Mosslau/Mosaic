apiVersion: 1

datasources:
  - name: Prometheus
    uid: mosaic-prometheus   # 面板 JSON 按 uid 引用; 注意: 已 provision 过的数据源改 uid 会导致启动失败(先删库内旧数据源)
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: true
