# 智能电动车数据智能平台 — 完整架构文档

---

## 一、总览架构图（分层版）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          数据源层 (Data Sources)                         │
├──────────┬──────────┬──────────┬──────────┬──────────┬─────────────────┤
│ 车端设备 │ 用户App  │ 售后系统 │ 生产系统 │ OTA系统  │ 充换电柜        │
│ (MQTT)   │ (HTTP)   │ (API)    │ (CDC)    │ (HTTP)   │ (MQTT/HTTP)     │
├──────────┴──────────┴──────────┴──────────┴──────────┴─────────────────┤
│ 文件(Excel/CSV/门店报表)  ·  多模态(损伤照片/通话录音/视频)           │
│ T+1 批量(售后维修记录 / 生产质检数据 / 运营报表)                        │
└────┬─────┴────┬─────┴────┬─────┴────┬─────┴────┬─────┴────┬────────────┘
     │          │          │          │          │          │
     ▼          ▼          ▼          ▼          ▼          ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      接入层 (Access Layer) — Go                          │
│                                                                         │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌──────────────────────┐ │
│  │ MQTT网关  │  │ HTTP网关  │  │ gRPC转发  │  │ TCP私有协议解析      │ │
│  └───────────┘  └───────────┘  └───────────┘  └──────────────────────┘ │
│                                                                         │
│  实时通道: 设备鉴权 | 限流 | 协议解析 | 背压控制 | 10万级并发           │
│  可观测: Prometheus metrics | pprof | OpenTelemetry trace               │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 离线通道: T+1 批量抽取(CDC/DataX/SeaTunnel) | 文件接收服务        │ │
│  │           多模态上传入口(工单损伤照片 / 客服录音)                 │ │
│  │ 铁律: 文件/多模态本体 → 对象存储(MinIO); 只有元数据+路径进 Kafka │ │
│  └───────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      消息总线 (Message Bus)                              │
│                                                                         │
│              Kafka / Pulsar / RocketMQ                                  │
│              Schema Registry (Avro / Protobuf / JSON Schema)            │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
          ┌────────────────────┼────────────────────┐
          ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────┐
│  实时计算层     │  │  离线计算层     │  │  CDC (Debezium/Flink CDC)   │
│  Flink          │  │  Spark / Hive   │  │  数据库变更捕获             │
├─────────────────┤  ├─────────────────┤  └─────────────────────────────┘
│ Flink SQL       │  │ Spark SQL       │
│ DataStream API  │  │ PySpark         │
│ 状态管理        │  │ Airflow 调度    │
│ Checkpoint      │  │ dbt 转换        │
│ Watermark       │  │ 数据质量检查    │
│ CEP 复杂事件    │  │ 任务治理        │
│ 窗口计算        │  │                 │
│ Exactly-Once    │  │                 │
└────────┬────────┘  └────────┬────────┘
         │                    │
         │    ┌───────────────┴────────────────────────────────────────┐
         │    │     AI 并行计算层 (Ray) — 📋 第 3 阶段启用              │
         │    │  批量推理 | embedding 生成 | 模型训练 | 超参搜索         │
         │    │  多模态预处理(照片/录音批量处理)                       │
         │    │  定位: AI 算力底座,消费湖仓数据、服务 AI 应用层,      │
         │    │  不是与 Flink/Spark 并列的第三数据计算引擎             │
         │    └───────────────┬────────────────────────────────────────┘
         │                    │ (产出: 风险评分/向量/多模态解析结果)
         └────────────────────┼────────────────────┐
                              ▼                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     湖仓层 (Lakehouse)                                   │
│                                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │
│  │ Iceberg/Hudi │  │ ClickHouse   │  │  Doris       │                  │
│  │ (湖仓表格式) │  │ (实时OLAP)   │  │ (高并发OLAP) │                  │
│  └──────────────┘  └──────────────┘  └──────────────┘                  │
│                                                                         │
│  存储: MinIO / S3 / HDFS  |  Metastore: Hive / Nessie / REST Catalog   │
│  格式: Parquet / ORC                                                     │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                      数据分层模型                                  │  │
│  │                                                                   │  │
│  │  ODS ────► DWD ────► DWS ────► ADS                              │  │
│  │  (原始)     (明细)     (汇总)     (应用)                           │  │
│  │                                                                   │  │
│  │  DIM: vehicle | user | battery | model | store | region           │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     查询与服务层 (Query & Service)                        │
│                                                                         │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌──────────────────────┐ │
│  │ Trino     │  │ ES        │  │ Redis     │  │ PostgreSQL           │ │
│  │ (联邦查询)│  │ (全文检索)│  │ (缓存)    │  │ (关系型)             │ │
│  └───────────┘  └───────────┘  └───────────┘  └──────────────────────┘ │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                   业务服务层 (Business Services) — Java                   │
│                                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │车辆档案  │ │电池资产  │ │故障码    │ │告警规则  │ │指标查询      │ │
│  │服务      │ │服务      │ │服务      │ │服务      │ │服务          │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │OTA管理   │ │售后工单  │ │用户服务  │ │诊断规则  │ │风控服务      │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────────┘ │
│                                                                         │
│  技术栈: Spring Boot | Nacos/Sentinel | Redis | Kafka                   │
│  MyBatis Plus | DDD领域建模 | 分布式事务 (本地消息表/Outbox)            │
│  统一: 异常处理 | 参数校验 | 鉴权 | TraceId | 单元测试                  │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                   AI应用层 (AI Applications) — Python                    │
│                                                                         │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────────────┐ │
│  │ 售后智能诊断助手 │ │ 数据分析Copilot │ │ 故障码解释助手           │ │
│  │                  │ │                  │ │                          │ │
│  │ RAG检索          │ │ Text-to-SQL      │ │ RAG + 知识库             │ │
│  │ Tool Calling     │ │ 指标问答         │ │ 多轮对话                 │ │
│  │ 多轮对话         │ │ 自动可视化       │ │ 引用来源                 │ │
│  │ 置信度评估       │ │ 业务建议         │ │                          │ │
│  └──────────────────┘ └──────────────────┘ └──────────────────────────┘ │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────────────┐ │
│  │ 电池风险分析     │ │ 运维Copilot     │ │ 智能客服/质检            │ │
│  └──────────────────┘ └──────────────────┘ └──────────────────────────┘ │
│                                                                         │
│  LLM: OpenAI / DeepSeek / Qwen  |  Framework: LangChain / LangGraph    │
│  Vector DB: Milvus / Qdrant      |  RAG: 混合检索 + 重排序             │
│  工程化: Prompt版本管理 | 评测集 | 幻觉检测 | 灰度发布 | 人工审核      │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       平台层 (Platform Layer)                            │
│                                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │数据接入  │ │数据开发  │ │实时计算  │ │指标管理  │ │标签管理      │ │
│  │平台      │ │平台      │ │平台      │ │平台      │ │平台          │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │特征管理  │ │数据质量  │ │数据血缘  │ │数据权限  │ │AI应用开发    │ │
│  │平台      │ │平台      │ │平台      │ │平台      │ │平台          │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    基础设施层 (Infrastructure)                            │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                      Kubernetes (K8s)                             │   │
│  │  Helm Chart | ConfigMap | Secret | HPA | Ingress                  │   │
│  │  灰度发布 | 服务探针 | 日志采集                                   │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                 可观测性 (Observability)                           │   │
│  │  OpenTelemetry (Traces + Metrics + Logs)                          │   │
│  │  Prometheus + Grafana + AlertManager                              │   │
│  │  pprof | flamegraph | 链路追踪                                    │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                 CI/CD + 安全                                       │   │
│  │  权限 (RBAC) | 审计 | 数据脱敏 | API网关 | SLA分级                │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    辅攻深度层 (Deep Tech Layer) — Rust/C++               │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  数据库内核理解 (C++/Rust)                                        │   │
│  │  • 阅读 RocksDB/LevelDB 存储引擎源码                              │   │
│  │  • 阅读 ClickHouse/DuckDB 查询执行源码                            │   │
│  │  • 理解 LSM Tree / B+Tree / WAL / MVCC / Compaction              │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  动手项目                                                         │   │
│  │  ① Rust KV Store → ② Mini LSM → ③ Mini Raft KV                  │   │
│  │  → ④ Mini SQL Engine → ⑤ 车联网时序KV原型                        │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  分布式系统基础                                                    │   │
│  │  Raft | 一致性哈希 | 分片 | Quorum | CAP | Exactly-Once          │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 二、企业级控制面架构图

```
┌──────────────────────────────────────────────────────────────────────┐
│                         企业级控制面 Control Plane                    │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐     │
│  │ 权限中心   │  │ 配置中心   │  │ 元数据中心 │  │ 指标中心   │     │
│  │ RBAC/ABAC  │  │ Nacos/Apollo│  │ DataHub    │  │ Metric Store│    │
│  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘     │
│        │               │               │               │            │
│  ┌─────▼──────┐  ┌─────▼──────┐  ┌─────▼──────┐  ┌─────▼──────┐     │
│  │ 任务调度   │  │ 数据质量   │  │ 血缘中心   │  │ 审计中心   │     │
│  │ Airflow    │  │ GreatExp   │  │ OpenLineage│  │ Audit Log  │     │
│  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘     │
│        │               │               │               │            │
│  ┌─────▼──────┐  ┌─────▼──────┐  ┌─────▼──────┐  ┌─────▼──────┐     │
│  │ 告警中心   │  │ 资源中心   │  │ 模型中心   │  │ 成本中心   │     │
│  │ AlertMgr   │  │ K8s/Quota  │  │ LLM/MLOps  │  │ FinOps     │     │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘     │
│                                                                      │
└──────────────────────────────────┬───────────────────────────────────┘
│
管控 / 策略 / 权限 / 审计 / SLA
│
▼
┌──────────────────────────────────────────────────────────────────────┐
│                         数据面 Data Plane                             │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  数据接入层     Kafka / Pulsar / RocketMQ                            │
│  实时计算层     Flink / Flink CDC                                    │
│  离线计算层     Spark / Hive / dbt                                   │
│  湖仓存储层     Iceberg / Hudi / MinIO / S3 / HDFS                   │
│  OLAP 查询层    ClickHouse / Doris / Trino                           │
│  服务层         Java 微服务 / Go 网关 / Python AI 服务                │
│  AI 应用层      RAG / Agent / Tool Calling / Copilot                 │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 控制面职责说明

| 模块 | 职责 |
|---|---|
| 权限中心 | 用户、角色、组织、租户、行列级权限、AI 工具调用权限 |
| 配置中心 | 服务配置、规则配置、灰度配置、告警阈值配置 |
| 元数据中心 | 表、字段、任务、接口、指标、模型、知识库元数据 |
| 指标中心 | 指标口径、指标版本、指标审批、实时/离线一致性 |
| 任务调度 | 离线任务、数据同步、质量检查、模型评测任务 |
| 数据质量 | 空值率、唯一性、完整性、时效性、波动检测、Schema Drift |
| 血缘中心 | 表血缘、字段血缘、任务血缘、指标血缘、AI 数据来源追踪 |
| 审计中心 | 数据访问审计、接口调用审计、AI 问答审计、权限变更审计 |
| 告警中心 | 任务失败、数据延迟、Kafka 积压、Flink 异常、模型异常 |
| 资源中心 | K8s 资源、队列资源、存储配额、计算资源、租户隔离 |
| 模型中心 | Prompt 版本、模型路由、RAG 评测、Embedding 管理、LLM Gateway |
| 成本中心 | 存储成本、计算成本、查询成本、模型调用成本、Token 成本 |

### 核心设计原则

企业级平台要区分：

```text
控制面负责“管控”
数据面负责“执行”
```

也就是：

* 控制面决定谁能用、怎么用、用多少、是否合规、是否稳定。
* 数据面负责真正的数据接入、计算、存储、查询和 AI 服务执行。

这样架构才从“项目系统”升级为“平台系统”。

---

## 三、数据流向简化版

```
  [车端设备/App/售后/OTA/充换电]   [文件/多模态/T+1 批量]
               │                            │
               ▼ (MQTT/HTTP/gRPC/TCP)        ▼ (CDC/DataX/文件服务;本体→MinIO)
  ┌─────────────────────────┐    ┌─────────────────────────┐
  │    Go 接入网关(实时)   │    │  离线接入通道           │  ← T+1 抽取/文件/多模态元数据
  └───────────┬─────────────┘    └───────────┬─────────────┘
              ▼                              │
  ┌─────────────────────────┐                │
  │    Kafka 消息总线       │◀───────────────┘ (元数据/CDC 事件)
  └─────┬──────────┬────────┘
        ▼          ▼
  ┌──────────┐ ┌──────────┐
  │  Flink   │ │  Spark   │  ← 实时+离线双引擎
  │  实时计算│ │  离线计算│
  └────┬─────┘ └────┬─────┘
       └──────┬─────┘
              ▼
  ┌─────────────────────────┐
  │ Iceberg / ClickHouse    │  ← 湖仓一体（ODS→DWD→DWS→ADS）
  │ MinIO(文件/多模态本体)│
  └───────────┬─────────────┘
              ▼
  ┌─────────────────────────┐
  │  Java 业务微服务        │  ← 车辆/电池/故障/告警/指标 API
  └───────────┬─────────────┘
              ▼
  ┌─────────────────────────┐
  │  Ray AI 并行计算层      │  ← 📋 第 3 阶段:批量推理/训练/embedding/多模态处理
  └───────────┬─────────────┘
              ▼
  ┌─────────────────────────┐
  │  Python AI 应用层       │  ← RAG 诊断 / Copilot 问数 / 风险分析 / 语音质检
  └───────────┬─────────────┘
              ▼
  ┌─────────────────────────┐
  │  业务决策闭环           │  ← 通知用户 → 触发售后 → 运营调整
  └─────────────────────────┘
```

---

## 四、语言职责边界

| 层次 | 语言 | 职责 | 核心产出 |
|------|------|------|----------|
| 接入层 | **Go** | 把数据"接进来" | 高并发网关、协议解析、设备鉴权 |
| 服务层 | **Java** | 把数据"管起来" | 业务微服务、事务、权限、指标 API |
| 智能层 | **Python** | 把数据"用起来" | RAG 诊断、Copilot、风险分析 |
| 深度层 | **Rust/C++** | 理解"底下是什么" | 存储引擎、分布式共识、执行模型 |

> 核心思想：不是一个人用五种语言，而是一个平台分五层，每层用最合适的语言。

---

## 五、业务能力闭环

```
业务问题
  ↓
数据建模          ← 哪些数据能回答这个问题？
  ↓
数据接入          ← Go 网关 + Kafka
  ↓
实时计算          ← Flink CEP / 窗口聚合
  ↓
湖仓沉淀          ← Iceberg + ClickHouse
  ↓
指标服务          ← Java API
  ↓
AI 应用           ← Python RAG + Tool Calling
  ↓
微服务工程化      ← K8s + OTel + Prometheus
  ↓
业务决策          ← 通知用户/售后/运营/安全
  ↓
持续优化          ← 数据质量 + 评测 + 反馈
```

### 闭环示例：电池安全风险预警

| 环节 | 具体动作 |
|------|----------|
| 业务问题 | 电池热失控如何提前发现？ |
| 数据接入 | 采集 BMS 电压、电流、温度、故障码 |
| 实时计算 | Flink 计算异常温升、过充、过放 |
| 数据沉淀 | Iceberg 存储历史，ClickHouse 支撑实时分析 |
| 指标服务 | Java 提供风险查询 API |
| AI 应用 | Python + 大模型解释风险原因和处理建议 |
| 工程化 | K8s 部署、OTel 追踪、Prometheus 告警 |
| 业务闭环 | 通知用户、售后、运营、安全团队 |

---

## 六、六核心项目依赖关系

```
项目1：实时数据平台（底座）──────────── 必须做
  ├── 项目2：电池风险预警（业务应用）──── 强烈建议做
  ├── 项目3：售后智能诊断（AI旗舰）──── 必须做
  ├── 项目4：数据分析Copilot（热点）──── 强烈建议做
  ├── 项目5：数据治理平台（工程化）──── 必须做
  └── 项目6：Rust时序KV（辅攻深度）──── 可选
```

---

## 七、24 个月架构演进节奏

| 阶段 | 时间 | 架构重点 | 新增能力 |
|------|------|----------|----------|
| 第 1 阶段 | 0-3 月 | 最小可用链路 | Go 网关 → Kafka → Flink → ClickHouse → Java API |
| 第 2 阶段 | 3-6 月 | 湖仓建模 + K8s | Iceberg 湖仓、数据分层 ODS→DWD→DWS→ADS、**离线接入(T+1 批量/文件/多模态 + 对象存储 MinIO)**、可观测性 |
| 第 3 阶段 | 6-12 月 | AI 应用接入 | RAG 管道、Vector DB、Tool Calling、评测体系、**Ray 并行计算(批量推理/训练/多模态处理)** |
| 第 4 阶段 | 12-18 月 | 平台化 | 10 大平台能力、数据资产地图、SLA 分级 |
| 第 5 阶段 | 18-24 月 | 技术品牌 | 辅攻深度项目、核心文章、代表项目输出 |
