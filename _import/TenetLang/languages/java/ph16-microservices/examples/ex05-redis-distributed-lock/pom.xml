<?xml version="1.0" encoding="UTF-8"?>
<!-- examples/ex05-redis-distributed-lock/pom.xml —— Redis 分布式锁（SET NX PX + Lua 原子释放）
     验证环境：OpenJDK 17.0.18 + Maven 3.9.12 + Spring Boot 3.3.0 + spring-data-redis 3.3.0 + lettuce 6.3.2.RELEASE
               （Boot 3.3.0 父 POM 管理的 lettuce 恰为 6.3.2.RELEASE，离线缓存内，无需仲裁）
     验证命令：mvn -o -Dmaven.repo.local=/tmp/m2clone test
               （测试用 ProcessBuilder 拉起真实 redis-server 8.6.2，随机端口，用完即关；
                找不到 redis-server 二进制时整组测试按「未在本环境验证」跳过——见 RedisServerSupport）
     离线说明：缓存缺 junit-platform-launcher 1.10.2（Boot 3.3.0 默认管理版本），钉到 1.10.1（离线必需）；
               surefire 3.2.5 显式钉住（恰为 Boot 3.3.0 默认管理版本，仅显式化，可删）。
               联网环境可删这些仲裁项。 -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.0</version>
    <relativePath/>
  </parent>
  <groupId>com.example</groupId>
  <artifactId>ex05-redis-distributed-lock</artifactId>
  <version>1.0.0</version>

  <properties>
    <java.version>17</java.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- 离线版本仲裁：缓存只有 junit-platform-launcher 1.10.1 -->
      <dependency>
        <groupId>org.junit.platform</groupId>
        <artifactId>junit-platform-launcher</artifactId>
        <version>1.10.1</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.data</groupId>
      <artifactId>spring-data-redis</artifactId>
    </dependency>
    <dependency>
      <groupId>io.lettuce</groupId>
      <artifactId>lettuce-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.platform</groupId>
      <artifactId>junit-platform-launcher</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <dependencies>
          <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <version>1.10.1</version>
          </dependency>
        </dependencies>
      </plugin>
    </plugins>
  </build>
</project>
