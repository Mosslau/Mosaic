<?xml version="1.0" encoding="UTF-8"?>
<!-- project/pom.xml —— 车辆数据上报 API：上报/查询/鉴权/校验/文档
     验证环境：OpenJDK 17.0.18 + Maven 3.9.12 + Spring Boot 3.3.0（本机离线 mvn -o 实测）
     验证命令：mvn -o -Dmaven.repo.local=/tmp/m2clone test
     运行命令：mvn -o -Dmaven.repo.local=/tmp/m2clone spring-boot:run（端口 18090，见 README）
     离线说明：本机缓存缺 junit-platform-launcher 1.10.2（Spring Boot 3.3.0 默认版本，缓存只有 1.10.1），
               pom 显式压到缓存里有的 1.10.1；surefire 3.2.5 与 spring-boot-maven-plugin 3.3.5 也在 pom 里显式钉住（缓存缺默认版本）。
               正常联网环境可删掉这些「离线版本仲裁」并让父 POM 自己管理。 -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.0</version>
    <relativePath/>
  </parent>
  <groupId>com.example</groupId>
  <artifactId>vehicle-report-api</artifactId>
  <version>1.0.0</version>

  <properties>
    <java.version>17</java.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- 离线版本仲裁：缓存只有 junit-platform-launcher 1.10.1 -->
      <dependency>
        <groupId>org.junit.platform</groupId>
        <artifactId>junit-platform-launcher</artifactId>
        <version>1.10.1</version>
      </dependency>
      <!-- 离线版本仲裁：springdoc/swagger 传递依赖声明的版本缓存里缺 jar，用缓存内的相近版本 -->
      <dependency>
        <groupId>jakarta.xml.bind</groupId>
        <artifactId>jakarta.xml.bind-api</artifactId>
        <version>3.0.1</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.dataformat</groupId>
        <artifactId>jackson-dataformat-yaml</artifactId>
        <version>2.15.3</version>
      </dependency>
      <dependency>
        <groupId>com.sun.activation</groupId>
        <artifactId>jakarta.activation</artifactId>
        <version>1.2.2</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- jjwt：JWT 签发/验签库（0.12.x API；对比 ex03 手写版） -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>0.12.5</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>0.12.5</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>0.12.5</version>
      <scope>runtime</scope>
    </dependency>
    <!-- springdoc-openapi：OpenAPI 3 文档 + Swagger UI（API 文档话题） -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.3.0</version>
    </dependency>
    <dependency>
      <groupId>org.junit.platform</groupId>
      <artifactId>junit-platform-launcher</artifactId>
      <version>1.10.1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <dependencies>
          <!-- 离线版本仲裁：surefire 的 junit-platform provider 需要 launcher，钉到缓存有的版本 -->
          <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <version>1.10.1</version>
          </dependency>
        </dependencies>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <!-- 离线版本仲裁：缓存缺 parent 管理的 3.3.0，钉到缓存有的 3.3.5；联网环境可删 version 由父 POM 管理 -->
        <version>3.3.5</version>
      </plugin>
    </plugins>
  </build>
</project>
