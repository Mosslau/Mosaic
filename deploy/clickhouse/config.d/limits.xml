<!-- OceanVerse ClickHouse 内存上限（2026-09-20 加，第 1、2 步评审整改）

     为什么**必须用配置文件**而不是环境变量:
       官方镜像只映射它认识的那一批 CLICKHOUSE_* 环境变量。实测
       `CLICKHOUSE_MAX_SERVER_MEMORY_USAGE=1000000000` 确实进了容器环境(`docker exec env` 可见),
       但 `system.server_settings` 里 max_server_memory_usage 仍是按 cgroup 推出来的值 ——
       即**配了等于没配**。配置文件是唯一可靠的落点。

     为什么要设这个上限:
       镜像默认 `max_server_memory_usage_to_ram_ratio=0.9`, 含义是"可用内存的 90%" ——
       在 6 GB 共享 VM 上等于没有上限；实测该容器稳态 MemoryResident ≈1.1 GiB，
       与 cgroup 上限贴到 94%，任何一次稍大的查询都会把它推到 OOM kill（表现为容器反复重启）。
       设了之后 ClickHouse 会**自己拒绝**超限查询(报 MEMORY_LIMIT_EXCEEDED), 而不是被杀。

     与 compose 的关系(成对约束, 改一个必须改另一个):
       mem_limit: 1280m(cgroup 硬杀线) > max_server_memory_usage: 1 GB(进程内)
       scripts/check-compose-budget.sh 会核对这条不等式。

     ⚠️ 不要在这里写 <max_server_memory_usage_to_ram_ratio>0</...>:
       实测该值 0 的语义是"**关闭内存上限**"(system.server_settings 会显示
       max_server_memory_usage=0 且 ratio=0, 即真的不限制), 与直觉相反。
       保持 ratio 为镜像默认(0.9)只作为兜底, 绝对值由上面的 max_server_memory_usage 决定。
-->
<clickhouse>
    <max_server_memory_usage>1000000000</max_server_memory_usage>
</clickhouse>
