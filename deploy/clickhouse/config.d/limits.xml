<!-- OceanVerse ClickHouse 内存上限

     为什么**必须用配置文件**而不是环境变量:
       官方镜像只映射它认识的那一批 CLICKHOUSE_* 环境变量。实测
       `CLICKHOUSE_MAX_SERVER_MEMORY_USAGE=1000000000` 确实进了容器环境(`docker exec env` 可见),
       但 `system.server_settings` 里 max_server_memory_usage 仍是按 cgroup 推出来的值 ——
       即**配了等于没配**。配置文件是唯一可靠的落点。

     为什么要设这个上限:
       镜像默认 `max_server_memory_usage_to_ram_ratio=0.9`, 含义是"可用内存的 90%" ——
       在共享 VM 上等于没有上限。上限的作用是让 ClickHouse **自己拒绝**超限查询
       (报 MEMORY_LIMIT_EXCEEDED), 而不是被 cgroup 硬杀(表现为容器反复重启)。

     上限必须**显著高于进程地板**（实测出来的, 别再设到贴近地板）:
       地板（重启后空闲）MemoryResident ≈ **830~870 MiB**;
       旧配置只给上限 953.67 MiB(1 GB) → 余量仅约 100 MiB, 于是:
       跑一段时间后 RSS 自己爬到 **1.16 GiB**（`MemoryTracking` 仅 236 MiB、缓存几乎为 0 ——
       涨的是 jemalloc 滞留页/碎片, 不是查询也不是缓存）, 一旦越过上限,
       OvercommitTracker 就会**拒绝一切还要内存的查询**（连 `SELECT count() FROM system.tables` 都拒），
       而 `SELECT 1` 照常返回 —— 表现为"服务活着但干不了活", 极具迷惑性。
       现取 **1536 MiB**: 高于地板约 670 MiB, 也高于"跑久后的 1.16 GiB"。
       现场止血: `docker compose restart clickhouse`（实测 1.16 GiB → 830 MiB, 查询立即恢复; 见 deploy/README Q17）。

     缓存边界必须显式声明（本文件下半部分）:
       镜像默认 mark_cache_size=5 GiB / index_mark_cache_size=5 GiB / uncompressed_cache_size=8 GiB /
       mmap_cache_size≈1 GiB。在上限只有 1.5 GiB 的配置下, 这些默认值是**隐性炸弹**:
       缓存与查询抢同一份额度, 直接触发上面那个 OvercommitTracker。
       显式设小 = 让行为可预测（与本仓"不依赖镜像默认值"的纪律一致, 同 Kafka 的 min.insync.replicas）。

     与 compose 的关系（成对约束, 改一个必须改另一个）:
       mem_limit: 2560m(cgroup 硬杀线) > max_server_memory_usage: 1.5 GiB(进程内) —— 余量 40%（1024 MiB）
       scripts/check-compose-budget.sh 核对两件事: ① 这条不等式; ② 下面四个缓存上限**是否显式声明**。

     不要在这里写 <max_server_memory_usage_to_ram_ratio>0</...>:
       实测该值 0 的语义是"**关闭内存上限**"(system.server_settings 会显示
       max_server_memory_usage=0 且 ratio=0, 即真的不限制), 与直觉相反。
       保持 ratio 为镜像默认(0.9)只作为兜底, 绝对值由上面的 max_server_memory_usage 决定。
-->
<clickhouse>
    <!-- 进程内上限 1536 MiB = 地板(≈850 MiB) + 工作空间(≈690 MiB) -->
    <max_server_memory_usage>1610612736</max_server_memory_usage>

    <!-- 缓存上限显式设小, 不让镜像默认的 5 GiB / 8 GiB 与查询抢内存 -->
    <mark_cache_size>134217728</mark_cache_size>                 <!-- 128 MiB（默认 5 GiB） -->
    <index_mark_cache_size>134217728</index_mark_cache_size>     <!-- 128 MiB（默认 5 GiB） -->
    <uncompressed_cache_size>0</uncompressed_cache_size>         <!-- 关闭（默认 8 GiB） -->
    <mmap_cache_size>134217728</mmap_cache_size>                 <!-- 128 MiB（默认约 1 GiB; 单位以 system.server_settings 复核为准） -->
    <compiled_expression_cache_size>33554432</compiled_expression_cache_size> <!-- 32 MiB（默认 128 MiB） -->
</clickhouse>
