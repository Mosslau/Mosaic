[package]
name = "tenet"
version = "0.1.0"
edition = "2021"
description = "Tenet — 从零实现的小型语言：lexer → parser → AST → 解释器 → Go 代码生成"
license = "MIT"

[lib]
name = "tenet"
path = "src/lib.rs"

[[bin]]
name = "tenet"
path = "src/main.rs"

[profile.release]
lto = true
