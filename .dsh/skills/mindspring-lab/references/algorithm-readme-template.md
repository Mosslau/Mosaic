# 算法实验 README 模板

> 用于 `algorithms/<NN-族>/<算法名>/README.md`。所有实验按此结构写，只替换占位内容。
> 本模板即权威结构，不指向仓库中任何具体文件（具体文件会重构，模板不过期）。

---

```markdown
# <算法名>

> 状态：⬜ 未开始 | 🚧 进行中 | ✅ 已完成（YYYY-MM-DD）
> 对应文档章节：`roadmap/人工智能代表算法演进路线.md` 第 <X.Y.Z> 章

<可选：一句话说明本实验所属算法族与目录形态，如"搜索求解器：本目录不是 sklearn 估计器，接口由算法自身形态决定">

## 设计原理

<这个算法要解决什么问题？核心思想一句话概括。它为什么比之前的方法好？>

## 数学推导

<关键公式与推导过程，手推一遍再写代码。公式中的符号要与 impl.py 的变量名对应>

## 目录形态（本算法的"类型"）（可选但推荐）

| 文件 | 角色 | 接口 |
|---|---|---|
| `impl.py` | 手写实现 | <函数签名> |
| `framework.py` 或 `baseline.py` | 对照版 | <函数签名> |
| `demo.py` | 双跑对比入口 | <输出什么指标> |

## 手写实现要点

<numpy / 纯 Python 手写实现时的关键步骤、容易踩的坑 —— 对应 impl.py>

## 框架对照 | 基线对照（段名按算法族二选一）

<框架对照：对应 sklearn / PyTorch 的接口是哪个？框架内部做了什么手写版没做的优化（正则化、向量化、数值稳定性）？>
<基线对照：基线算法是哪个（Dijkstra / 随机策略 / 无剪枝版）？对照想量化什么收益？>

## 实验结果

<数据集/环境参数（含随机种子）>

| 指标 | 手写版 | 对照版 | 结论 |
|---|---|---|---|
| <指标 1> | <实数> | <实数> | <差异与原因> |
| 耗时 | <实数> | <实数> | `[随环境/版本变化]` |

复现方式：<命令>（本目录内）。验证状态声明按三态规范写足证据。

<可视化结论（如有）：从图/输出中观察到的实质现象>

## 局限与延伸

- <这个算法的边界在哪里？>
- <它引出了哪个后续算法？带相对链接，如 `../<算法名>/`>
```

---

## 填充示例（自含示例，不指向任何存量文件）

- 「实验结果」段：`地图参数：21×21 网格，30% 障碍率，固定种子保证连通` + 指标表（路径长度 47 vs 47 → 最优性断言通过；扩展节点 214 vs 239 → 启发式收益量化）+ `复现方式：python3 demo.py`
- 「局限与延伸」段：每条是一个方向 + 一句话理由 + 相对链接（如 `../<后续算法>/`），把实验串成学习路径
- 耗时类数字标注 `[随环境/版本变化]`，避免被当成永久事实

## 写作要点

1. **数学推导先行**：推导没写完不动手写 impl.py。推导段是给自己看的证明，不是装饰
2. 「设计原理」回答"为什么需要它"——放在演进路线里看（它取代了谁、被谁取代）
3. 「对照」段的重点是**差异原因分析**：框架多做了什么（向量化、正则化、数值稳定性），手写版差在哪
4. 「实验结果」的每个数字必须来自实际运行的输出；跑不了就标「未在本环境验证」并说明缺什么
5. 状态推进到 ✅ 的门槛：六段无占位、双跑对照有实数、验证声明三要素齐全
