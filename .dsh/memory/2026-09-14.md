# 2026-09-14

基线：main @ f6c9b15（与远程 origin/main 同步，远程：git@github.com:Mosslau/MindSpring.git；本次 a-star 变更为未提交工作区，5 改 1 增）

## a-star 实验修正与深化（algorithms/01-search/a-star）

背景：实验原标 ✅（2026-08-29），但存在两段跨行括号占位（validate.py 单行正则不覆盖，漏检）+ 验证声明不符纪律⑤（`python3 demo.py` 在系统 Python 3.9 下实测报 TypeError）。经自评深检后按 A→B→C 优先级全部修正。

### 已完成

- **A1 措辞精确性**：手写实现要点坑句从"不可采纳的 h"修正为"可采纳但不一致的 h 同样失效"（无重开 closed 依赖的是一致性）
- **A2 口径统一**：实验一表格拆为路径节点数（47）与路径代价（46）两行，注明断言锁的是 `path_cost`
- **B3 平局破解实验**（demo 实验二）：`impl.solve()` 新增 `tie_break` 参数（insertion/large_g）；无障碍图 361 vs 37（理论下界）、30% 障碍图 214 vs 199。机制：无障碍时曼哈顿 h 处处紧、所有自由节点 f ≡ C*，扩展量全由平局策略兑现
- **B4 最优效率定理**入数学推导段：A* 必扩展所有 f(n) < C* 的节点（Dechter & Pearl 1985），效率不确定性集中在 f = C* 边界集 → 解释 B3 现象
- **B5 加权 A* 实验**（demo 实验三）：`solve()` 新增 `weight` 参数；w=1.0/2.0/3.0/5.0 → 扩展 199/141/131/133、代价 46/46/48/58，w=3 起真次优
- **C6 测试**：新增 `test_impl.py` 15 例（10 seed 参数化最优性、无障碍下界、无解、输入校验、w=1 一致性、加权有解性），`.venv` 下全过（0.07s）
- **C7 输入校验**：`solve()` 对越界/障碍起终点抛 ValueError
- 验证声明重写为三要素格式（含 .venv 路径、≤3.9 兼容性警告、pytest 命令）；完成日期更新为 2026-09-14 并同步索引表

### 待升级项（存量偏差记录）

- `validate.py` 的 `PLACEHOLDER_RE` 只匹配单行闭合括号，跨行占位漏检 → 建议改跨行匹配
- `validate.py --pytest` 用 `sys.executable`：系统 python3（3.9）跑会因无 pytest 误报硬伤，需用 `.venv/bin/python` 运行该脚本
- 全仓 pytest 有 6 个**既有**失败，位于 `engineering/06-agent-nest/tutorials/python/tests/test_compaction_tool_pairs.py`，与本次变更无关，待 06-agent-nest 线处理
- 其余 20+ 个 ⬜ 实验 README 仍为模板占位（正常，未开始状态）

### 环境备注

- `.venv` 无 pip（uv 管理）；装包用 `uv pip install --no-cache <pkg>`（沙箱挡 `~/.cache/uv`）
- 本次新装：pytest 9.1.1（pyproject dev 依赖原已声明）
