# 蒙特卡洛树搜索 MCTS

> 状态：⬜ 未开始
> 对应文档章节：`roadmap/人工智能代表算法演进路线.md` 第 2.2.1 章

搜索求解器：本目录不是 sklearn 估计器，接口由"模拟搜索"自身的形态决定。

## 设计原理

（这个算法要解决什么问题？核心思想一句话概括。它为什么比之前的方法好？）

## 数学推导

（UCB1 公式与探索-利用权衡；置信上界的由来；手推一遍再写代码）

## 目录形态（本算法的"类型"）

| 文件 | 角色 | 接口 |
|---|---|---|
| `impl.py` | 手写 MCTS | `MCTS(get_moves, apply, is_terminal, winner).search(state, iterations) -> action` |
| `baseline.py` | 随机策略对照 | `random_move(state, get_moves) -> action` |
| `demo.py` | 对战收敛实验 | 迭代次数 → 先手胜率曲线 |

## 手写实现要点

（UCB1 选择；随机 rollout；回传更新；探索常数 C 的影响；
固定随机种子保证可复现——纯 Python 实现，无需 numpy）

## 基线对照

（对照对象是纯随机策略：MCTS 本质是"用模拟次数换决策质量"，
胜率随迭代次数的收敛即是对照结论；结果记录到下方「实验结果」）

## 实验结果

（棋盘/迭代次数参数、评价指标、可视化结论）
（**MCTS vs 随机基线**：不同迭代次数下的胜率、耗时差异、差异原因分析）

## 局限与延伸

（这个算法的边界在哪里？它引出了哪个后续算法？）
