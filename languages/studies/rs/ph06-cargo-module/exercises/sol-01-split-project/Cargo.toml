# exercises/sol-01-split-project/Cargo.toml —— 练习 1 参考实现：温度记录统计项目
# 拆分自 exercises/ex01-single-source.rs，依赖方向 main -> service -> parser -> model
# 验证环境：rustc 1.92.0 + cargo 1.92.0，纯标准库
# 编译：cargo build；运行：cargo run；测试：cargo test（在本目录执行）
# 验证状态：已验证（rustc 1.92.0）
[package]
name = "temp-stats"
version = "0.1.0"
edition = "2021"

[dependencies]
