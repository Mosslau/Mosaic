# exercises/sol-03-unit-tests/Cargo.toml —— 练习 3 参考实现：整数统计库（单元测试 + 质量门禁）
# 验证环境：rustc 1.92.0 + cargo 1.92.0，纯标准库
# 质量门禁：cargo fmt --check && cargo clippy -- -D warnings && cargo test（在本目录执行）
# 验证状态：已验证（rustc 1.92.0）
[package]
name = "int-stats"
version = "0.1.0"
edition = "2021"

[dependencies]
