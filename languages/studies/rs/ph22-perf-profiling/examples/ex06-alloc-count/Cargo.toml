[package]
name = "ex06-alloc-count"
version = "0.1.0"
edition = "2021"
publish = false

# ex06：分配计数的四组写法对照 —— 复用缓冲 / String vs &str / with_capacity / 小向量。
# 验证环境：rustc/cargo 1.92.0，无第三方依赖。
# 验证状态：已验证（本机实测分配次数见 README 与主文档；计数分配器同 ex03 那份，
#   独立复制以便各自可跑）。
# 构建/运行命令（在示例目录内执行）：
#   CARGO_TARGET_DIR=/tmp/ph22-ex06-target cargo build --release
#   /tmp/ph22-ex06-target/release/ex06-alloc-count
# 质量闸门：
#   CARGO_TARGET_DIR=/tmp/ph22-ex06-target cargo fmt --check
#   CARGO_TARGET_DIR=/tmp/ph22-ex06-target cargo clippy --all-targets -- -D warnings
#   CARGO_TARGET_DIR=/tmp/ph22-ex06-target cargo test
