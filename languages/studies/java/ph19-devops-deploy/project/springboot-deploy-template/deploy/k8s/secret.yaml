# project/springboot-deploy-template/deploy/k8s/secret.yaml
# 敏感配置（教学明文；base64 只是编码，生产用外部 Secret 方案）。验证状态：未在本环境实际构建验证
apiVersion: v1
kind: Secret
metadata:
  name: myapp-secret
  namespace: myapp
type: Opaque
stringData:
  DB_PASSWORD: "app123"
  REDIS_PASSWORD: "redis123"
