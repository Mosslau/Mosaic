# exercises/sol-04-k8s-deploy/configmap.yaml —— 练习 4 参考实现：非敏感配置（对应主文档 3.5）
# 验证状态：未在本环境实际构建验证
apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-config
  namespace: myapp
data:
  SPRING_PROFILES_ACTIVE: "k8s"
  DB_URL: "jdbc:mysql://mysql:3306/myapp?useSSL=false&serverTimezone=UTC&characterEncoding=utf8"
  DB_USERNAME: "app"
  REDIS_HOST: "redis"
