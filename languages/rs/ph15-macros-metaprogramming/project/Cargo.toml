[package]
name = "event-hub"
version = "0.1.0"
edition = "2021"
description = "ph15 综合项目：事件结构体宏 + serde/thiserror derive"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
